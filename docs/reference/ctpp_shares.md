# Add shares to CTPP table

Add shares to CTPP table

## Usage

``` r
ctpp_shares(df)
```

## Arguments

- df:

  dataframe, either result of get_psrc_ctpp or psrc_ctpp_stat

## Value

dataframe with share & share_moe
